<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >

    <include
        android:id="@+id/include_toolbar_income_detail"
        layout="@layout/base_toolbar"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/padding_size_24x"
        >

        <TextView
            android:id="@+id/fragment_income_detail_date_text_view"
            style="@style/TitleTextBold"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_size_30x"
            tools:text="Втроник, 10 декабря"
            />

        <TextView
            android:id="@+id/fragment_income_detail_cost_text_view"
            style="@style/TitleTextBold"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_size_16x"
            android:textColor="@color/income_text_color"
            android:textSize="@dimen/text_size_30x"
            tools:text="3 290 тг."
            />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/medium_divider_height"
        android:background="@color/large_divider_bg_color"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/fragment_income_detail_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:orientation="vertical"
        android:overScrollMode="never"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_shop_order_clothes"
        />

    <!--    <include layout="@layout/item_profile_income_item_detail" />-->

    <!--    <include layout="@layout/item_profile_income_item_detail" />-->
</LinearLayout>